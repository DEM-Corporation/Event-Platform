<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/main_background"
    android:layout_height="match_parent"
    tools:context=".bottomnav.home.HomeFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp"
        android:background="@color/main_background"
        android:id="@+id/chatsRecView"/>

    <ImageView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:src="@drawable/ic_neutral_face_two_color"
        android:id="@+id/neutral_face_image_id_messenger"
        android:visibility="invisible"
        android:layout_marginTop="120dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <TextView
        android:id="@+id/no_chats_textview"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="No chats"
        android:textSize="20sp"
        android:textColor="@color/simple_text"
        android:visibility="invisible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/neutral_face_image_id_messenger"
        />


    <TextView
        android:id="@+id/no_chats_textview2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create event or register for the event."
        android:visibility="invisible"
        android:textColor="@color/simple_text"
        android:layout_marginTop="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/no_chats_textview" />


</androidx.constraintlayout.widget.ConstraintLayout>